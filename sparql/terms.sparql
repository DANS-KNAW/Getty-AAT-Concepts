
# Get the different types of terms in the AAT vocabulary

PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT DISTINCT ?o
FROM <http://vocab.getty.edu/dataset/aat>
WHERE {
    ?s rdf:type ?o .
}
LIMIT 100
